# Copy to .env.local for local development; loaded by `pnpm dev` (run from the repo root).
# Any variable already set in your shell wins over this file.
ARGUS_API_URL=http://localhost:8000/api/v1
ARGUS_HUB_URL=http://localhost:3000
# Worker image to run
ARGUS_WORKER_IMAGE=argusaudit/worker:0.1.0
